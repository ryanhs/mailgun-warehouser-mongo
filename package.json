{
  "name": "mailgun-warehouser",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "description": "Mailgun log warehouser",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "sync": "ts-node-dev --transpile-only src/index.ts",
    "expired-router-fetch": "ts-node-dev --transpile-only src/expired-router-fetch.ts"
  },
  "dependencies": {
    "dotenv": "^16.1.4",
    "luxon": "^3.7.2",
    "mongodb": "^6.12.1"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
